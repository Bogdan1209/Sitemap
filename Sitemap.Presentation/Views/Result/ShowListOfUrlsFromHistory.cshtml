@model dynamic
@{
    ViewBag.Title = "ShowListOfUrlsFromHistory";
}

<h2>ShowListOfUrlsFromHistory</h2>

<table id="historiesTable" class="table table-hover">
    <thead>
        <tr>
            <th>Url</th>
            <th>Min Value</th>
            <th>Max Value</th>
            <th>Current Value</th>
        </tr>
    </thead>
    <tbody id="resultBody">
        @foreach (var history in Model)
        {
            <tr>
                <td>@history.Url</td>
                <td>@history.MinValue</td>
                <td>@history.MaxValue</td>
                <td>@history.CurrentValue</td>
            </tr>
        }
    </tbody>
</table>